specVersion: "v1"
serviceId: "testapp"
location: "asia-southeast1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "testapp-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./example"]
